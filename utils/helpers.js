module.exports = {
    format_time: date => {
        // return `${new Date(60 * 3600000).toISOString().substr(11, 8)}`
        
        // return `${new Date(date).getMonth() + 1}/${new Date(date).getDate()}/${new Date(date).getFullYear()}`

    },
}